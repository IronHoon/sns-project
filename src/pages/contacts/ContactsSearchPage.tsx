import React from 'react';
import ContactsSearch from 'views/contacts/ContactsSearch';

function ContactsSearchPage({ navigation }) {
    return <ContactsSearch navigation={navigation} />
}

export default ContactsSearchPage;
