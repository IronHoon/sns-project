import React from 'react';
import ScanResult from 'views/contacts/ScanResult';

function ScanResultPage({ navigation }) {
    return <ScanResult navigation={navigation} />
}

export default ScanResultPage;
